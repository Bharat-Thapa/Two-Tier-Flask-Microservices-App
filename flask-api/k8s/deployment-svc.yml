apiVersion: v1
kind: Service
metadata:
  name: deployment-svc
spec:
  selector:
    app: deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
  type: NodePort
